<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/dlMain"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <include layout="@layout/main_container"/>

    <!--<include layout="@layout/main_menu"/>-->

    <include android:id="@+id/BackGroundWhenChoice" layout="@layout/halph_transp_background" android:visibility="gone"/>

    <FrameLayout
        android:id="@+id/flMainContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">
        <LinearLayout
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:clickable="true"
            android:focusable="true"
            android:layout_gravity="right">
            <ImageView
                android:id="@+id/ChooseModuleButtonTime"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:visibility="invisible"
                android:layout_gravity="center_vertical"
                android:layout_marginStart="5dp"
                android:clickable="true"
                android:src="@drawable/ic_v7logo" />
        </LinearLayout>

        <ImageView
            android:id="@+id/gotoMoneyButton"
            android:layout_width="85dp"
            android:layout_height="82dp"
            android:layout_gravity="top|right"
            android:layout_marginRight="15dp"
            android:layout_marginTop="75dp"
            android:clickable="true"
            android:visibility="invisible"
            app:srcCompat="@android:drawable/presence_online" />

        <ImageView
            android:id="@+id/gotoGoalButton"
            android:layout_width="85dp"
            android:layout_height="82dp"
            android:layout_gravity="top|right"
            android:layout_marginRight="100dp"
            android:layout_marginTop="15dp"
            android:clickable="true"
            android:visibility="invisible"
            app:srcCompat="@android:drawable/presence_online" />
    </FrameLayout>

</android.support.constraint.ConstraintLayout>